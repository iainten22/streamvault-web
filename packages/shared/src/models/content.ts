export enum ContentType {
  LIVE = "live",
  VOD = "vod",
  SERIES = "series",
}

export enum ServerStatus {
  ACTIVE = "active",
  EXPIRED = "expired",
  DISABLED = "disabled",
  BANNED = "banned",
  UNKNOWN = "unknown",
}
